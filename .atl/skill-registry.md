# Skill Registry — MetaProject

> Auto-generated by sdd-init on 2026-05-05.
> This file maps project-relevant skills to their triggers so agents know what to load.

## Project Context
- **Frontend**: Angular 17 (standalone, bootstrapApplication, lazy routes), TypeScript 5.4, Tailwind CSS 3, Angular Material 17, ngx-toastr
- **Backend**: ASP.NET Core 8 Web API, EF Core 8 (SQLite), JWT Bearer auth, Swashbuckle Swagger
- **Other**: Docker, nginx, THREE.js (3D backgrounds)
- **Testing**: Frontend: Karma+Jasmine (`ng test`), Backend: NONE (no test project)

## Relevant Skills (Project-Level)

| Skill | Trigger | Why |
|-------|---------|-----|
| `aspnet-core` | ASP.NET Core, Web API, .NET controllers, Program.cs, DI, middleware | .NET 8 backend uses controllers + DI |
| `dotnet-backend-patterns` | .NET backend, EF Core, async/await, xUnit, API patterns | Reviewing C# code, API architecture |
| `frontend-design` | Build UI, component, page, styling | Angular frontend needs design work |
| `ui-ux-pro-max` | UI/UX review, design system, layout, typography | Design decisions for Angular app |
| `web-design-guidelines` | Review UI, check accessibility, audit design | Ensure frontend quality |
| `architecture-patterns` | Clean Architecture, Hexagonal, DDD | Backend could benefit from refactoring |
| `analyzing-dotnet-performance` | Optimize .NET, allocations, hot paths | Performance review of .NET API |
| `code-testing-agent` | Generate tests, unit tests, coverage | Adding tests to Angular/.NET |
| `adapt` | Responsive, mobile, breakpoint | Angular app needs responsive design |

## Compact Rules (Auto-Resolved)

### aspnet-core
- Use Program.cs for DI and middleware pipeline
- Controllers with `[ApiController]` and route attributes
- Use `IConfiguration` for settings, not hardcoded values
- Prefer `async/await` for all I/O operations

### dotnet-backend-patterns
- Use repository pattern or direct DbContext via DI
- Keep controllers thin — business logic in services
- Use `IServiceCollection` extension methods for modular registration
- Handle errors with middleware, not try-catch in every controller

### frontend-design (Angular)
- Use Angular Material components consistently
- Follow standalone component pattern (no NgModules)
- Use Tailwind utility classes for layout
- Keep components small and focused

### code-testing-agent
- Frontend: Generate `.spec.ts` files for Karma/Jasmine
- Backend: Needs xUnit test project setup first
- Coverage: Available for frontend via `ng test --coverage`

## Agent Skills (SDD Workflow)

| Skill | Purpose |
|-------|---------|
| `sdd-init` | Initialize SDD context |
| `sdd-explore` | Investigate ideas |
| `sdd-propose` | Create change proposals |
| `sdd-spec` | Write specifications |
| `sdd-design` | Technical design |
| `sdd-tasks` | Break down tasks |
| `sdd-apply` | Implement changes |
| `sdd-verify` | Validate changes |
| `sdd-archive` | Archive completed changes |
| `sdd-onboard` | Onboard users to SDD |

## How to Update
Run `skill-registry` skill or re-run `sdd-init` to regenerate this file after installing new skills.